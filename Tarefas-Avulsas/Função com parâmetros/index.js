function dividirNumeros(numero1, numero2) {

    const resultado = numero1 / numero2;
    console.log(`O resultado da divisão foi ${resultado}`);
}

// Chamada da função com dois números como parâmetros
dividirNumeros(10, 2);
